#include<stdio.h>
#include<stdlib.h>
#include<conio.h>
enum HeroID { plute, engineer, sporter };
enum weatherID { sunny, rain, wind, snow };
enum timeID { day, night };
enum calamityID { thunder, earthquake, typhoon };
struct HeroProperty {
  int HPmax;
  int MPmax;
  int Health;
  int cash;
  int arms;
  enum HeroID heroid;
};

int main() {
void startmenu();

}

void startmenu(){
	
	
	}